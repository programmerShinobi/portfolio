# Assets to replace before deploy

- `cv-faqih-pratama-muhti.pdf` — placeholder; replace with the real CV.
- `og-image.png` — add a 1200x630 share image (not committed as binary here).
- `favicon.svg` — simple placeholder mark; replace if desired.
