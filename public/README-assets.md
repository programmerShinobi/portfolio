# Assets

- `cv-faqih-pratama-muhti.pdf` — the CV (source of truth for content).
- `og-image.png` — 1200×630 link-share image, generated by
  `scripts/generate-og-image.py` (run `python3 scripts/generate-og-image.py` to
  regenerate). Replace with a custom design if desired.
- `favicon.svg` — simple placeholder mark; replace if desired.
